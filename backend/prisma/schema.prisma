generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:IN49KBWYHzXM@ep-weathered-darkness-a6roilpc.us-west-2.aws.neon.tech/mini_minds?sslmode=require"
}

model Resource {
  id          Int      @id @default(autoincrement())
  title       String
  type        String   // e.g., book, video, game
  subject     String   // e.g., math, art, reading
  ageGroup    String   // e.g., 3-4, 4-5, 5-6
  rating      Int
  imageUrl    String?  // Renamed from link to imageUrl
  description String?  // Optional description for the resource
  eventDate   DateTime // New field for event date
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
